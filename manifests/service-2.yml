apiVersion: v1
kind: Service
metadata:
    name: "ascicdk8s-01f4"
    labels:
        app: "ascicdk8s-01f4"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "ascicdk8s-01f4"